{
  "name": "dynamic-workflow-automation",
  "version": "1.0.0",
  "description": "Workflow Automation System",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --names \"API,WORKER,UI\" --prefix-colors \"cyan,yellow,magenta\" \"npm run dev --prefix backend\" \"npm run worker:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
